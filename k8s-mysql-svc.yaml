apiVersion: apps/v1
kind: Service
metadata:
  name: sba-mysql
  namespace: sba
  labels:
    name: sba-mysql
spec:
  externalIPs:
    - 10.0.75.66
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    name: sba-mysql